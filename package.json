{
  "name": "cypress-the-internet-ui",
  "private": true,
  "scripts": {
    "test": "cypress run --headless --browser electron --reporter junit --reporter-options mochaFile=results/junit-[hash].xml,toConsole=true",
    "open": "cypress open",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "axe-core": "^4.10.3",
    "cypress": "^13.17.0",
    "cypress-axe": "^1.7.0",
    "prettier": "^3.3.3"
  }
}
